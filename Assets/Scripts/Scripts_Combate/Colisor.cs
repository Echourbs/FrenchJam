using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Colisor : MonoBehaviour
{
    /*
    //este codigo é para testes, apagar depois
    private void OnTriggerEnter2D(Collider2D c)
    {
        if (c.gameObject.GetComponent<EnemyStatus>())
        {
            c.gameObject.GetComponent<EnemyStatus>().CastDamage(25);
        }
    }
    */
}
